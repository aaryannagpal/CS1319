int main() {
    int x = 5;
    
    // Missing closing parenthesis
    if (x > 0 {
        printStr("x is positive.\n");
    }

    // Missing semicolon
    int y = 10
    printInt(y);

    // Undefined variable
    z = 15;

    return 0;
}

